# SAP 网关地址覆盖（仅其它设备用；本机=能连 SAP 的 A 无需此文件）
#
# 用法：连不通 SAP 的设备，复制本文件为 .env.local，把下面地址改成「能连 SAP 的 A」的转发地址，
# 再 npm run dev。.env.local 已被 .gitignore 忽略，不会进 git、不影响别人。
# 不填则用 sap.config.js 里的默认真 SAP 地址。
#
# 前缀 → 变量名对应：/sap-dev → SAP_DEV_TARGET、/sap-test → SAP_TEST_TARGET
# 值 = A 的 IP + A 上转发到对应 SAP 端口的端口（A 需监听 0.0.0.0 且防火墙放行）。

SAP_DEV_TARGET=http://192.168.1.100:18400
SAP_TEST_TARGET=http://192.168.1.100:18010
